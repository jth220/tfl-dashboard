# Optional for local anonymous TfL access; recommended in production.
TFL_API_KEY=

# Optional. Defaults to the repository's data directory.
# Set this to a mounted shared volume when API and worker are separate services.
TFL_DATA_DIR=
